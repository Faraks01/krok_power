version: "3"

services:
  backend:
    build: ./krok_power_backend
    volumes:
      - ./krok_power_backend/:/krok_power_backend # maps host diretory to internal container directory
    working_dir: /krok_power_backend/
    command: sh entrypoint.sh
  nginx:
    image: nginx:latest
    ports:
      - 89:8080
    volumes:
      - ./nginx/nginx-proxy-just-front.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - backend
